import SwiftUI

struct SettingsTab: View {
    var body: some View {
        NavigationStack {
            Text("Coming soon")
                .font(.title2)
                .foregroundColor(.secondary)
                .navigationTitle("Settings")
        }
    }
}

#Preview {
    SettingsTab()
}
