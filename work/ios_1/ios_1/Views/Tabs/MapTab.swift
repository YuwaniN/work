import SwiftUI

struct MapTab: View {
    var body: some View {
        NavigationStack {
            Text("Coming soon")
                .font(.title2)
                .foregroundColor(.secondary)
                .navigationTitle("Map")
        }
    }
}

#Preview {
    MapTab()
}
