import Foundation

struct NotificationService {
    func requestPermission() {
        // Stub — will request UNUserNotificationCenter authorization in a later step
    }

    func scheduleDaily(at hour: Int, minute: Int) {
        // Stub — will schedule a daily notification in a later step
    }
}
